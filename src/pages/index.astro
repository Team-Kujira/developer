---
import { getEntryBySlug } from "astro:content";
import Layout from "../layouts/Layout.astro";

const post = await getEntryBySlug("docs", "index");
const { Content, headings } = await post.render();
---

<Layout {post}>
  <Content />
</Layout>
